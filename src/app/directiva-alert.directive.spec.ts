import { DirectivaAlertDirective } from './directiva-alert.directive';

describe('DirectivaAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaAlertDirective();
    expect(directive).toBeTruthy();
  });
});
