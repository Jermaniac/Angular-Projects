import { ValidarNifDirective } from './validar-nif.directive';

describe('ValidarNifDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarNifDirective();
    expect(directive).toBeTruthy();
  });
});
