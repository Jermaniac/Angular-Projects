import { FiltroPreciosPipe } from './filtro-precios.pipe';

describe('FiltroPreciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPreciosPipe();
    expect(pipe).toBeTruthy();
  });
});
